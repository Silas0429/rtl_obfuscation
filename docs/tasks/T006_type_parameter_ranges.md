# T006：单个 type parameter 映射与类型引用范围

- 状态：`SUPERSEDED`
- 设计负责人：主 Agent
- 实现负责人：未启动
- 前置任务：T005 已达到 `ACCEPTED`

T006 是从未进入 `READY` 的历史草案，没有产生实现、测试或产品行为。其 `type_parameters` 提案
不属于当前公开 category，原草案中的命令、输入和输出合同不再是开发入口，也不应继续照此启动。

如果未来重新需要 type parameter 支持，必须由主 Agent 创建新的任务合同，重新定义范围、行为和验收；
本任务不可恢复为 `READY`。
